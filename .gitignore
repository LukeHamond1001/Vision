__pycache__/
*.pyc
.DS_Store

# secrets — never commit
API Key.md
*.key

# launch-private (playbooks stay local)
docs-private/
data/

# per-run scratch target of scripts/life_gate.py (per-arm copies are committed as *_<arm>.json)
results/evidence/v10_gates_epi.json
