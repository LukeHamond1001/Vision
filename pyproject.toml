[project]
name = "iga"
version = "0.1.0"
description = "Imagination-Gated Agent — reference scaffold for SPEC.md"
requires-python = ">=3.11"
dependencies = ["torch>=2.0", "numpy>=1.24"]

[tool.setuptools]
packages = ["iga", "iga.envs"]
